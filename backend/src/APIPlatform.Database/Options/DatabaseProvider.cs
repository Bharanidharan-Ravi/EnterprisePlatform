namespace APIPlatform.Data.Options;

/// <summary>Identifies which database engine a connection targets.</summary>
public enum DatabaseProvider
{
    SqlServer,
    Hana,
    Sqlite,
    PostgreSql,
    Oracle
}
